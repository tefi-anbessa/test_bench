---
trigger: always_on
---

Use bootstrap_form gem for forms whenever possible. That is, always unless explicitly permitted otherwise for functionality reason.
This means form labels don't need to be defined unless they are non-standard.