<%% provide(:title, t('.title')) %>
<div class="container mt-3" style="<%%= container_styles(@swatch) %>">
  <!-- <%= human_name %> Header Card -->
  <div class="card mb-4" style="<%%= card_styles(@swatch) %>">
    <div class="card-header p-2" style="<%%= card_header_styles(@swatch) %>">
      <div class="row align-items-center">
        <!-- Left Navigation -->
        <div class="col-auto">
          <div class="d-flex align-items-center">
            <!-- Index Button -->
            <%% if policy(@<%= singular_name %>).index? %>
              <%%= nav_button(action: :index, path: <%= index_path(@nesting) %>, record: @<%= singular_name %>) %>
            <%% end %>

            <!-- Previous Button -->
            <%%%= nav_button(action: :previous, record: @neighbours[0]) %>
          </div>
        </div>

        <!-- Center Badge Info -->
        <div class="col text-center">
          <div class="d-flex flex-column align-items-center">
            <div class="mt-1">
              <span class="badge fs-6" style="<%%= badge_styles(@swatch) %>">
                <%%= t('.header', label: @<%= singular_name %>.label)%>
              </span>
              <!-- Add descriptor here if available -->
            </div>
          </div>
        </div>

        <!-- Right Navigation and Actions -->
        <div class="col-auto">
          <div class="d-flex align-items-center">
            <!-- Next Button -->
            <%%= nav_button(action: :next, record: @neighbours[1]) %>

            <!-- Action Buttons -->
            <div class="btn-group btn-group-sm gap-1">
              <%% if policy(@<%= singular_name %>).edit? %>
                <%%= nav_button(action: :edit, record: @<%= singular_name %>, path: edit_<%= singular_route_name %>_path(@<%= singular_name %>)) %>
              <%% end %>

              <%% if policy(@<%= singular_name %>).destroy? %>
                <%%= nav_button(action: :delete, record: @<%= singular_name %>) %>
              <%% end %>

              <%% if policy(@<%= singular_name %>).new? %>
                <%%= nav_button(action: :new, path: <%= new_path(@nesting) %>, record: @<%= singular_name %>) %>
              <%% end %>
            </div> <!-- button group -->
          </div> <!-- d-flex align-items-center -->
        </div> <!-- right navigation and actions -->
      </div> <!-- row align-items-center -->
    </div> <!-- card header -->

    <div class="card-body">

      <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-8 col-xl-6">

          <!-- <%= model_class_name %> Details Card -->
          <div class="card mb-4" style="<%%= card_styles(@swatch) %>">
            <div class="card-header py-1" style="<%%= card_header_styles(@swatch) %>">
              <h4 class="mb-0"><%%= t('show.details', model: @<%= singular_name %>.model_name.human) %></h4>
            </div>

            <div class="card-body p-3">
<%- @attribute_fields.each do |field| -%>
  <%- options = field[:options].slice(:precision, :scale, :units, :si) -%>
  <%- opts = options.map { |k, v| "#{k}: #{v.inspect}" }.join(", ") -%>
  <%- case field[:type] -%>
  <%- when :string -%>
              <%%= show_attribute(@<%= singular_name %>, :<%= field[:name] %>) %>
  <%- when :integer, :bigint, :text, :jsonb, :boolean, :date, :datetime, :timestamp, :time, :enum, :enum_translated -%> 
              <%%= show_attribute(@<%= singular_name %>, :<%= field[:name] %>, type: :<%= field[:type] %>) %>
  <%- when :float, :decimal -%>
              <%%= show_attribute(@<%= singular_name %>, :<%= field[:name] %>, type: :<%= field[:type] %><%= ", #{opts}" unless opts.empty? %>) %>
  <%- when :binary -%> 
                <div class="col-8"><%%= 'PLACEHOLDER FOR BINARY FIELD' %></div>
  <%- else -%> 
              <%%= show_attribute(@<%= singular_name %>, :<%= field[:name] %>) %>
  <%- end -%>
<%- end -%>
            </div>   <!-- temporary end inner card body -->

            <!-- Associations -->
            <div class="card-header py-1" style="<%%= card_header_styles(@swatch) %>">
              <h4 class="mb-0"><%%= t('show.associations', model: @<%= singular_name %>.class.model_name.human) %></h4>
            </div>

            <div class="card-body p-3" style="<%%= card_styles(@swatch) %>">
<%- case @nesting -%>
<%- when :tag, :discipline, :project -%>
              <%%= show_association(@<%= singular_name %>, :<%= @nesting %>) %>
<%- when :tagable -%>
              <%%= show_association(@<%= singular_name %>, :tag) %>
<%- end -%>
<%- @association_fields.each do |field| -%>
              <%%= show_association(@<%= singular_name %>, :<%= field[:name] %>) %>
<%- end -%>
            </div>  <!-- end inner card body -->
          </div> <!-- end inner card -->
        </div> <!-- end responsive columns -->
      </div> <!-- justify-content-center -->
    </div> <!-- end outer card body -->
  </div> <!-- end card -->
</div> <!-- end container -->
