<%% provide(:title, t('.title')) %>
<%% provide(:header, t('.header', label: @<%= singular_name %>.long_label )) %>
<%% provide(:button_text, t('actions.update')) %>
<%%= render "form",
  <%= singular_name %>: @<%= singular_name %>,
  tag: @tag,
  discipline: @discipline,
  swatch: @swatch
%>