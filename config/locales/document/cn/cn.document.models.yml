# document models translations for cn
cn:
  activerecord:
    models:           # Insert model name translations here
    attributes:       # Insert model name translations here
    errors:           # Insert custom validation error translations here
