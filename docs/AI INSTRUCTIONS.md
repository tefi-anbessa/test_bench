# AI INSTRUCTIONS

## Overview
This document provides a list of instructions which have to be repeated to AI very frequently.

- NEVER suggest using a CDN for code.
- NEVER suggest using rspec for testing.
- NEVER suggest using fixtures, the project is now using factory_bot.
- NEVER suggest using anything other than bootstrap for styling.
