<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: [@circuit.switchboard&.label, @circuit&.label].join(': '))) %>
<% provide(:button_text, t('actions.save')) %>
<%= render "form", 
  switchboard: @switchboard,
  circuit: @circuit
%>
