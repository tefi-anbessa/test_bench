<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @circuit.long_label)) %>
<% provide(:button_text, t('actions.update')) %>
<%= render "form", 
  switchboard: @switchboard,
  circuit: @circuit
%>
