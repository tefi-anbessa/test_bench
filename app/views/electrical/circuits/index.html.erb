<% provide(:title, t('.title')) %>
<div class="container mt-3 mx-auto" style="<%= container_styles(@swatch) %>">

  <!-- Circuit Schedule Card Header -->
  <div class="card mb-1" style="<%= card_styles(@swatch) %>">
    <div class="card-header p-1" style="<%= card_header_styles(@swatch) %>">
      <div class="row align-items-center p-2 mb-1">

        <!-- Switchboard or Discipline link -->
        <div class="col-2">
          <% if @switchboard.present?%>
            <%= nav_button(action: :up, record: @switchboard) %>
          <% else %>
            <%= nav_button(action: :show_discipline, record: @discipline) %>
          <% end %>
        </div>

        <!-- Header Text (Left - 8 columns) -->
        <div class="col-8">
          <% scope_text = @switchboard.present? ? 
            [t("activerecord.models.electrical/switchboard.one"), @switchboard.long_label].join(' ') : 
            t('index.all', models: t('activerecord.models.electrical/switchboard.other')) %>
          <h5><%= t('.header', scope_text: scope_text) %></h5>
        </div>

        <!-- New Button (Right) -->
        <div class="col-2 align-content-end">
          <% if @switchboard.present? && policy(@switchboard).new? %>
            <%= nav_button(action: :new, path: new_electrical_switchboard_circuit_path(@switchboard),
                record: @switchboard.circuits.new) %>
          <% end %>
        </div> 
      </div> <!-- row -->
    </div> <!-- end of card header -->

    <div class="card-body">
      <div class="row search-form">
        <%= search_form_for @q, 
        url: @switchboard.present? ? 
              electrical_switchboard_circuits_path(@switchboard) :
              discipline_electrical_circuits_path(@discipline) do |f| %>
          <%= f.search_field :phase_cont,
                class: "search_field",
                placeholder: t('activerecord.attributes.electrical/circuit.phase') %>
          <%= f.button nil, class: "btn btn-outline-primary btn-sm mb-1", action: :submit do %>
            <%== bs_icon('search') %>
          <% end %>
        <% end %>
      </div> <!-- end of search-form -->

      <!-- Circuits -->
      <!-- Policies for circuits are inherited from switchboard, and are invariable over the scope,
      so no need to look up policy for each record. Use a dummy tag in the discipline. -->
      <% can_show = policy(@discipline.tags.build()).show? %>
      <% can_edit = policy(@discipline.tags.build()).edit? %>
      <% can_delete = policy(@discipline.tags.build()).destroy? %>
      
      <table id='circuits' class='table table-responsive table-condensed table-striped table-hoverable table-bordered' >
        <%= render 'header' %>
        <tbody>
          <!-- [TODO - orphans go here] -->
          <% @circuits.each do |row| %>
            <%= render 'row', row: row, can_show: can_show, can_edit: can_edit, can_delete: can_delete %>
          <% end %>
        </tbody>
      </table>
    </div> <!-- end of card-body -->
    <div class="card-footer">
      <%== pagy_bootstrap_nav(@pagy) %>
      <%== pagy_info(@pagy) %>
    </div>
  </div> <!-- end of card -->
</div> <!-- end of container -->
