<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @switchboard.long_label)) %>
<% provide(:button_text, t('actions.update')) %>
<%= render "form", 
  tagable: @switchboard,
  tag: @tag,
  parent: @tag,
  discipline: @discipline,
  swatch: @swatch,
  circuits: @circuits
%>
