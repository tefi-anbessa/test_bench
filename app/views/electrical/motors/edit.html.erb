<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @motor.long_label)) %>
<% provide(:button_text, t('actions.update')) %>
<% partial = File.join(@motor.model_name.collection, "form") %>
<%= render partial,
  motor: @motor,
  tag: @tag,
  parent: @tag,
  discipline: @discipline,
  swatch: @swatch
%>
