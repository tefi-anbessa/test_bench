<thead>
  <% unless current_project %>
    <th scope="col"><%= sort_link(@q, 'tag_discipline_project_code', Project.model_name.human) %></th>
  <% end %>
  <th scope="col"><%= sort_link(@q, :tag_stage) %></th>
  <th scope="col"><%= sort_link(@q, :tag) %></th>
  <th scope="col"><%= sort_link(@q, :socket_type) %></th>
  <th scope="col"><%= sort_link(@q, :quantity) %></th>
  <th scope="col"><%= t('activerecord.models.electrical.demand') %></th>
  <th scope="col" width="150px"><%= t('table.links')%></th>
</thead>