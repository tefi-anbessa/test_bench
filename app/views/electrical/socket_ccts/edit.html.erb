<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @socket_cct.long_label)) %>
<% provide(:button_text, t('actions.update')) %>
<% partial = File.join(@socket_cct.model_name.collection, "form") %>
<%= render partial,
  socket_cct: @socket_cct,
  tag: @tag,
  parent: @tag,
  discipline: @discipline,
  swatch: @swatch
%>
