<!-- Heater collapsible card -->
<!-- Expecting 'object', 'context' and 'id' variables, passed from the shared/_association partial. -->
<!-- The card uses the context name in the header and the id for the block (for testing). -->
<% heater = object %>
<% context ||= t("activerecord.models.electrical/heater.one") %>
<%= render 'components/collapsible',
  id: "#{id}_#{heater.id}_details",
  title: [context, heater.long_label].join(': '),
  expanded: false,
  header_class: 'py-1' do %>
  
    <div class="container-fluid">
      <%= nav_link(action: :show, record: heater) %>

      <%= show_attribute(heater, :heater_type, type: :enum_translated) %>
      <%= show_attribute(heater, :application, type: :enum_translated) %>
      <%= show_attribute(heater, :ingress_protection) %>
      <%= show_attribute(heater, :sheath_temperature_max, type: :float, units: "°C") %>
      <%= show_attribute(heater, :power_density_min, type: :float, units: "W/m²") %>
      <%= show_attribute(heater, :power_density_max, type: :float, units: "W/m²") %>
      <%= show_attribute(heater, :sheath_material, type: :enum_translated) %>
      <%= show_attribute(heater, :insulation_material, type: :enum_translated) %>
      <%= show_attribute(heater, :notes, type: :text) %>

    </div> <!-- end container-fluid -->
<% end %>