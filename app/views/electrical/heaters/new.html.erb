<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', scope_text: @discipline.long_label)) %>
<% provide(:button_text, t('actions.create')) %>
<% partial = File.join(@heater.model_name.collection, "form") %>
<%= render partial,
  heater: @heater,
  parent: @parent,
  discipline: @discipline,
  tag: @tag,
  swatch: @swatch
%>
