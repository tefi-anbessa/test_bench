<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @motor.label)) %>
<% provide(:button_text, t('actions.update')) %>
<%= render "form",
  motor: @motor,
  tag: @tag,
  voltage_ratings: @voltage_ratings
%>
