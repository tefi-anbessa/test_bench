<thead>
  <% unless current_project %>
    <th scope="col"><%= sort_link(@q, 'project_code', t("activerecord.models.project.one")) %></th>
  <% end %>
  <th scope="col"><%= sort_link(@q, :serial) %></th>
  <th scope="col"><%= sort_link(@q, :title) %></th>
  <th scope="col"><%= sort_link(@q, :duration) %></th>
  <th scope="col" width="150px"><%= t('table.links')%></th>
</thead>
