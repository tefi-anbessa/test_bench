<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @tag.label)) %>
<% provide(:button_text, t('actions.update')) %>
<%= render 'form',
      tag: @tag,
      discipline: @discipline,
      schema: @schema,
      parts: @parts,
      parents: @parents %>
