<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', discipline: @discipline.code)) %>
<% provide(:button_text, t('actions.create')) %>
<%= render 'form', 
      tag: @tag,
      discipline: @discipline,
      schema: @schema,
      parts: @parts,
      parents: @parents %>
