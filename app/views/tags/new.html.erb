<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', discipline: @discipline.label)) %>
<% provide(:button_text, t('actions.create')) %>
<%= render 'form', 
          tag: @tag,
          project: @project,
          projects: @projects,
          disciplines: @disciplines %>
