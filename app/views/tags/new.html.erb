<% provide(:title, t('.title')) %>
<% provide(:button_text, t('actions.save')) %>
<%= render 'form', 
          tag: @tag,
          project: @project,
          projects: @projects,
          disciplines: @disciplines %>
