<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @issue.label )) %>
<% provide(:button_text, t('actions.update')) %>
<%= render "form",
  issue: @issue,
  document: @document,
  swatch: @swatch
%>