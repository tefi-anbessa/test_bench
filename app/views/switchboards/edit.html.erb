<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', label: @switchboard.label)) %>
<% provide(:button_text, t('actions.save')) %>
<%= render "form", 
  switchboard: @switchboard,
  tag: @tag,
  circuits: @circuits,
  voltage_ratings: @voltage_ratings,
  ip_1: @ip_1,
  ip_2: @ip_2
%>
