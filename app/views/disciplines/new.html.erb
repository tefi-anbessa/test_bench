<% provide(:title, t('.title')) %>
<% provide(:header, t('.header', scope_text: t("projects.show.header", label: @project.label))) %>
<% provide(:button_text, t('actions.create')) %>
<%= render "form",
  discipline: @discipline,
  project: @project
%>
