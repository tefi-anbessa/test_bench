<thead>
  <th scope="col"><%= sort_link(@q, :vendor) %></th>
  <th scope="col"><%= sort_link(@q, :title) %></th>
  <th scope="col"><%= sort_link(@q, :revision) %></th>
  <th scope="col"><%= sort_link(@q, :file_extension) %></th>
  <th scope="col" width="150px"><%= t('table.links')%></th>
</thead>
