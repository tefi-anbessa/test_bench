<!-- Project collapsible card -->
<% project = object %>
<div class="card-body">
	<%= render 'components/collapsible',
		title: [t("activerecord.models.project"), project.label].join(": "),
		id: 'project-collapse',
		expanded: false,
		show_icon: true,
		header_class: 'py-1' do %>  
			<div class="row mb-2">
				<div class="col-12">
					<div class="d-flex align-items-center">
						<span class="text-muted me-2">
							<%= link_to [t('activerecord.models.project'), project.label].join(': '), 
								project_path(project), 
								class: 'text-decoration-none' %>
						</span>
					</div>
				</div>
			</div>
			<div class="row mb-2">
				<div class="col-12">
					<div class="d-flex align-items-center">
						<span><%= [t("activerecord.attributes.project.title"), project.title].join(': ') %></span>
					</div>
				</div>
			</div>
			<div class="row mb-2">
				<div class="col-12">
					<div class="d-flex align-items-center">
						<span><%= [t("activerecord.attributes.project.description"), project.description].join(': ') %></span>
					</div>
				</div>
			</div>
			<div class="row mb-2">
				<div class="col-12">
					<div class="d-flex align-items-center">
						<span><%= [t("activerecord.models.swatch"), project.swatch&.name].join(': ') %></span>
					</div>
				</div>
			</div>
		<% end %>
</div> 